from django.apps import AppConfig


class AntigenapiConfig(AppConfig):
    """Django application configuration for antigenapp API."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "antigenapi"
